completion_signal:
  skill: pb-v1-designing
  status: completed
  artifacts:
    - path: docs/iterations/ltx-video-service/architecture.md
      type: architecture
    - path: docs/iterations/ltx-video-service/arch_decisions.md
      type: arch-decisions
  issues:
    - id: DSGN-UPSTREAM-001
      description: 缺少 proposal.md、feature-spec-index.md、feature-specs/ 和 prd_review.md；本次架构以 clarifications 派生 P0 能力。
      severity: MAJOR
      points_to_upstream: true
      gate_candidate: G2
    - id: DSGN-CAPACITY-001
      description: GPU 型号、显存分布和 LTX 2.3 实测耗时不进入 Phase 1；Phase 2 接入 GPU 前需要压测。
      severity: MINOR
      points_to_upstream: false
      gate_candidate: G4
    - id: DSGN-ROADMAP-001
      description: 用户补充要求分阶段完成；Phase 1 已收窄为非 GPU 控制面，GPU 相关进入 Phase 2。
      severity: MINOR
      points_to_upstream: false
  assumptions:
    - clr_id: CLR-ARCH-024
      summary: 第一阶段对外只提供 API，通过 API Key 做资源隔离。
    - clr_id: CLR-ARCH-016
      summary: GPU 集群采用 Kubernetes + NVIDIA GPU Operator，但进入 Phase 2。
    - clr_id: CLR-ARCH-017
      summary: 每张 GPU 一个 ComfyUI Worker，单任务单卡；Phase 2 实现。
    - clr_id: CLR-ARCH-018
      summary: 模型走节点本地 NVMe/PVC 缓存，输入输出走对象存储抽象。
    - clr_id: ROADMAP-OPEN-SOURCE
      summary: Phase 1 采用 FastAPI、PostgreSQL、MinIO 和控制面基础指标；GPU/K8s/ComfyUI/LTX/DCGM 进入 Phase 2；Kong、Redis/Temporal、KEDA 后置。
