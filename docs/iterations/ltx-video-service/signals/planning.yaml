completion_signal:
  skill: pb-v1-planning
  status: completed
  scope: "Phase 2 GPU execution engineering plan"
  artifacts:
    - path: docs/iterations/ltx-video-service/tasks.md
      type: tasks
  issues:
    - id: PLAN-UPSTREAM-001
      description: 缺少正式 proposal/feature-specs 和 arch_review.md；Phase 2 任务规划基于 clarifications、architecture 和 arch_decisions 派生能力。
      severity: MAJOR
      points_to_upstream: true
      gate_candidate: G2
    - id: PLAN-CAPACITY-001
      description: GPU 型号、显存、磁盘容量、LTX 2.3 8 Worker 并发耗时仍需在 T-205/T-209 实测。
      severity: MINOR
      points_to_upstream: false
      gate_candidate: G4
  assumptions:
    - clr_id: CLR-ARCH-024
      summary: 对外只提供 API，通过 API Key 做资源隔离。
    - clr_id: CLR-ARCH-016
      summary: Kubernetes + NVIDIA GPU Operator 保留为多机或 Phase 2.5 路径。
    - clr_id: CLR-ARCH-017
      summary: 单任务单卡，每张 GPU 一个 ComfyUI Worker。
    - clr_id: CLR-ARCH-018
      summary: 模型走本地缓存，输入输出走对象存储抽象。
    - clr_id: CLR-ARCH-029
      summary: Phase 2 首版按一台 8 卡 GPU 服务器、默认 8 Worker 验收；Worker 数量必须可配置。
    - clr_id: CLR-ARCH-030
      summary: Phase 2 可先用本地共享存储，也可用 MinIO，但必须经 ObjectStorageAdapter。
    - clr_id: CLR-ARCH-031
      summary: Phase 2 首个真实 LTX profile 使用 distilled single-stage；two-stage/upscale 后置。
    - clr_id: CLR-ARCH-032
      summary: Phase 2 必须新增 gpu_server/，作为 GPU 节点部署子项目。
    - clr_id: ROADMAP-OPEN-SOURCE
      summary: Phase 2 首版采用 gpu_server/、Docker Compose、NVIDIA Container Toolkit、ComfyUI、ComfyUI-LTXVideo、DCGM/Worker 指标；Kong、Redis/Temporal、KEDA 后置。
