completion_signal:
  skill: pb-v1-planning
  status: completed
  artifacts:
    - path: docs/iterations/ltx-video-service/tasks.md
      type: tasks
  issues:
    - id: PLAN-UPSTREAM-001
      description: 缺少 feature-specs/ 和 arch_review.md，本规划以 architecture.md、arch_decisions.md 和用户新增 MVP 目标为输入。
      severity: MAJOR
      points_to_upstream: true
      gate_candidate: G2
    - id: PLAN-SCOPE-001
      description: 用户将第一阶段收窄为非 GPU 控制面；规划解释为 1 个 web/control 节点，不处理 GPU 服务器及相关逻辑。
      severity: MINOR
      points_to_upstream: false
    - id: PLAN-CAPACITY-001
      description: GPU 型号、显存、LTX 2.3 实测耗时不进入 Phase 1；Phase 2 容量与性能验收需要 T-002/T-017 实测。
      severity: MINOR
      points_to_upstream: false
      gate_candidate: G4
    - id: PLAN-ROADMAP-001
      description: Phase 1 规划已调整为中期目标架构的控制面切片；运行件可简化，但 Edge Gateway、QueueAdapter、ObjectStorageAdapter、ExecutorAdapter 边界必须保留。
      severity: MINOR
      points_to_upstream: false
  assumptions:
    - clr_id: CLR-ARCH-024
      summary: 第一阶段对外只提供 API，通过 API Key 做资源隔离。
    - clr_id: CLR-ARCH-016
      summary: GPU 集群采用 Kubernetes + NVIDIA GPU Operator；进入 Phase 2。
    - clr_id: CLR-ARCH-017
      summary: 单任务单卡，每张 GPU 一个 ComfyUI Worker；进入 Phase 2。
    - clr_id: CLR-ARCH-018
      summary: 模型走本地缓存，输入输出走对象存储抽象。
    - clr_id: ROADMAP-OPEN-SOURCE
      summary: Phase 1 默认运行件为 FastAPI、PostgreSQL、MinIO 和控制面基础指标；Kubernetes/k3s、NVIDIA GPU Operator、ComfyUI、ComfyUI-LTXVideo、DCGM Exporter 进入 Phase 2；Kong、Redis/Temporal、KEDA 后置。
