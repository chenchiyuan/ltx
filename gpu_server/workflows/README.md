# Workflows

T-205 will add pinned LTX 2.3 distilled single-stage Workflow API JSON files here.

Do not place ComfyUI source-format workflows in this directory for runtime execution. The Worker Adapter will consume Workflow API Format JSON only.
